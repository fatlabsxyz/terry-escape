// import { LobbyMsg } from "client/types";
import { Server } from "socket.io";

export function addLobby(server: Server): Server {
  // server.on(LobbyMsg.PING, (socket) => {
  //   console.log("PING", socket.id)
  // })
  return server
}
